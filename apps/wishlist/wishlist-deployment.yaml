apiVersion: apps/v1
kind: Deployment
metadata:
  name: wishlist
  namespace: wishlist
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: wishlist
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: wishlist
    spec:
      containers:
      - name: wishlist
        image: ghcr.io/coultonf/wishlist:v0.64.1-slate.1
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: wishlist-config
        ports:
        - containerPort: 3280
          protocol: TCP
        resources:
          requests:
            memory: 128Mi
          limits:
            memory: 512Mi
        livenessProbe:
          tcpSocket:
            port: 3280
          initialDelaySeconds: 20
          periodSeconds: 30
        readinessProbe:
          tcpSocket:
            port: 3280
          initialDelaySeconds: 10
          periodSeconds: 10
        # Direct mounts only — NO subPath. On this Talos node, kubelet
        # materializes subPath bind sources inside its own container overlay
        # (missing extraMount), so subPath data silently evaporates on
        # kubelet/sandbox restarts. Bit us twice: 2026-07-01 and 2026-07-06.
        volumeMounts:
        - mountPath: /usr/src/app/data
          name: wishlist-data
        - mountPath: /usr/src/app/uploads
          name: wishlist-uploads
      restartPolicy: Always
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/control-plane
        operator: Exists
      volumes:
      - name: wishlist-data
        persistentVolumeClaim:
          claimName: wishlist-data
      - name: wishlist-uploads
        persistentVolumeClaim:
          claimName: wishlist-uploads
