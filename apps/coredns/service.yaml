apiVersion: v1
kind: Service
metadata:
  name: coredns
  namespace: coredns
  labels:
    app.kubernetes.io/name: coredns
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: coredns
  ports:
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
