apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: icloud-photos
  namespace: photo-sync
spec:
  accessModes:
    - ReadWriteOnce  # local-path supports single-node access
  resources:
    requests:
      storage: 100Gi  # Adjust based on iCloud library size
  storageClassName: local-path
