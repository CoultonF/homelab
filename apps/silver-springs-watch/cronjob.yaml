apiVersion: batch/v1
kind: CronJob
metadata:
  name: silver-springs-watch
  namespace: silver-springs-watch
  labels:
    app.kubernetes.io/name: silver-springs-watch
spec:
  schedule: "5 * * * *"
  timeZone: America/Edmonton
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  startingDeadlineSeconds: 600
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 300
      template:
        metadata:
          labels:
            app.kubernetes.io/name: silver-springs-watch
        spec:
          restartPolicy: OnFailure
          tolerations:
            - effect: NoSchedule
              key: node-role.kubernetes.io/control-plane
              operator: Exists
          containers:
            - name: watcher
              image: python:3.12-slim
              command: ["sh", "-c"]
              args:
                - |
                  set -e
                  pip install --quiet --no-cache-dir requests cryptography
                  python3 /app/watcher.py
              env:
                - name: HOUSE_SIGMA_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: silver-springs-secrets
                      key: house-sigma-token
                - name: SMTP_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: silver-springs-secrets
                      key: smtp-password
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  memory: 512Mi
              volumeMounts:
                - name: script
                  mountPath: /app
                  readOnly: true
                - name: state
                  mountPath: /state
          volumes:
            - name: script
              configMap:
                name: watcher-script
                defaultMode: 0555
            - name: state
              persistentVolumeClaim:
                claimName: silver-springs-state
