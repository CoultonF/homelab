apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: icloudpd-config-stephanie
  namespace: photo-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
